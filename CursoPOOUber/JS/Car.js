class Car {
    constructor(licence, drive) {
        this.id;
        this.licence = licence;
        this.drive = drive;
        this.passenger;
    }
    printDataCar() {
        console.log(this.drive)
        console.log(this.drive.name)
        console.log(this.drive.document)
    }
}
